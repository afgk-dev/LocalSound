package dev.afgk.localsound.ui.tracks

import androidx.lifecycle.ViewModel
import dev.afgk.localsound.data.playlists.PlaylistRepository

class TrackPopupMenuViewModel(
    playlistRepository: PlaylistRepository
) : ViewModel() {
}